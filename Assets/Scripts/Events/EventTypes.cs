using UnityEngine;
using System.Collections;

public enum EventTypes {
    playerForward,
    playerBackwards,
    playerLeft,
    playerRight,

    cameraUp,
    cameraDown,
    cameraLeft,
    cameraRight
}